# hunmin-multilingual-encoder

Hunmin encoder experiment repository.

This repo stores reproducible code/config/docs only. Large datasets, checkpoints,
and generated outputs are intentionally excluded and tracked through runbooks,
checksums, and artifact archives.
